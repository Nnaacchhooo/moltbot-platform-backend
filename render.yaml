services:
  - type: web
    name: moltbot-platform-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm run dev
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
